name: Build Addressables

on:
  pull_request:
    branches: [master]
    paths:
      - 'Assets/**'
  push:
    branches: [master]
    paths:
      - 'Assets/**'
  workflow_dispatch:

concurrency:
  group: build-addressables-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  build-addressables:
    uses: ./.github/workflows/unity-ci-reusable.yml
    secrets: inherit
    with:
      build_webgl: false
      build_addressables: true
      deploy_webgl: false
      upload_addressables: ${{ github.event_name != 'pull_request' }}
